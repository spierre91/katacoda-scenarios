import { Command } from '@oclif/command';
export default class ScenariosCreate extends Command {
    static description: string;
    private generateIndexFile;
    private generateScenarioContents;
    private getScenarioDetails;
    run(): Promise<void>;
}
