import { Command, flags } from '@oclif/command';
export default class StepRemove extends Command {
    static description: string;
    static flags: {
        scenariopath: flags.IOptionFlag<string | undefined>;
        steps: flags.IOptionFlag<string | undefined>;
    };
    private getStepDetails;
    private getStepsToBeRemoved;
    private deleteStepFiles;
    private updateScenarioIndexFile;
    private reIndexFiles;
    run(): Promise<undefined>;
}
