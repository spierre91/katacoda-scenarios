import { Command, flags } from '@oclif/command';
export default class AddAssets extends Command {
    static description: string;
    static flags: {
        asset: flags.IOptionFlag<string | undefined>;
        target: flags.IOptionFlag<string | undefined>;
    };
    private getAssetDetails;
    private getOverwriteResponse;
    private updateScenarioDetails;
    run(): Promise<void>;
}
