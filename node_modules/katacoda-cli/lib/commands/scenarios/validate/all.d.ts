import { Command, flags } from '@oclif/command';
export default class ScenariosValidateAll extends Command {
    static flags: {
        repo: flags.IOptionFlag<string | undefined>;
        ignore: flags.IOptionFlag<string | undefined>;
    };
    static description: string;
    private getRepoPath;
    run(): Promise<void>;
}
