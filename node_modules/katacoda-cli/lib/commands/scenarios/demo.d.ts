import { Command } from '@oclif/command';
export default class ScenariosDemo extends Command {
    static description: string;
    private confirmOverwrite;
    private generateUniqueScenarioPath;
    private generateDemo;
    run(): Promise<void>;
}
