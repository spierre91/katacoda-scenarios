import { Command } from "@oclif/command";
export default class CoursesCreate extends Command {
    static description: string;
    private createPathwayFile;
    private getCourseDetails;
    run(): Promise<void>;
}
